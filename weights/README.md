# Weights

Store local model weights and exported artifacts here when needed.

This directory is ignored by Git except for this README. Do not commit `.pt`, `.onnx`, `.bin`, `.engine`, or other large model artifacts.
